# STATE.min
JWT operador: OK (HS256 via /auth/pin, Bearer+X-Pin compat). Auth OBRIGATÓRIA nas mutações: atendimentos (create/status/edit), payments (charge/stub), servicos CRUD, lavadores CRUD, reclamações criar — via Depends(get_current_lavador). Abertos: GETs, seed, health, providers, caixa, pin-do-dia, auth/pin. Payments: manual + stubs pix_manual/cartao_pos no registry, charge pluggable, sem Asaas. POST atendimentos sem lavador_id no body vincula o lavador do token/PIN. Salto UX: PATCH /atendimentos/{id} edita placa/servico apenas na_fila (422 fora dela); AtendimentoOut inclui servico_nome e lavador_nome; CaixaDiaOut com por_lavador; GET /servicos?ativos=false admin; POST /lavadores/{id}/pin-do-dia → {pin, qr_payload}. Web Salto UX M1–M4: painel CRUD/status/receber manual inline, atender sem foto, caixa com data+stats+tabela+fila, serviços/lavadores/configurações novas. Deploy: .github/workflows/deploy.yml — push/merge main → CI gate → SSH VPS (git reset --hard origin/main + docker compose up -d --build, preserva .env) → healthcheck /health. Secrets VPS_* ausentes (CD bloqueado no SSH; VPS não tocada). Próximo: CEO cadastrar VPS_* nos secrets do repo; depois DNS / provedores reais.

Log 2026-08-25: Deploy YAML corrigido (PR fix 2026-08-25): Telegram sem secrets.* em if:; healthcheck só `/health`. Pipeline parseia OK; job deploy falha em `Preparar chave SSH` porque secrets `VPS_SSH_KEY`/`VPS_HOST`/`VPS_USER` estão vazios/ausentes — VPS NÃO tocada. Produção estável (smoke /health=200). CEO: cadastrar secrets VPS_* (e opcional TELEGRAM_*) nos 4 repos.

- **Log 2026-08-25:** branch claude/ceo-ready-20260825 — light/dark + gaps CEO-ready; builds/tests verdes; prod ainda depende de secrets VPS_*.

- **Log 2026-08-25 (gaps uso real):** branch claude/gaps-uso-real. `POST /lavadores/{id}/pin-do-dia` agora exige auth (Depends get_current_lavador); bootstrap do primeiro login vem de `bootstrap_pin` retornado por `/seed` (continua aberto). PIN do dia agora é único globalmente (não só por lavador) — `app/pin_service.py` sorteia e tenta de novo em colisão, com constraint `uq_pin_dia_global(data,pin)` de defesa. `/auth/pin` rejeita (403) PIN de lavador `ativo=false`. `PATCH /atendimentos/{id}/status` não deixa mais pular `pronto→pago` (422) — pagamento só via `POST /payments/charge`. Web: `/painel` filtra `?data=hoje` + status abertos por padrão (checkbox revela pagos/cancelados); `apiFetch` em 401 limpa sessão e redireciona `/entrar`; desativar serviço/lavador pede confirmação; `/lavadores` tem botão Copiar PIN. Testes API 25/25 verdes; build web verde.
